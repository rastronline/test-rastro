<div class="card p-1 mx-1 my-2" style="max-width: 300px">
  <a href="/articles/{{ article.id }}">
    <div>
      <img src="/uploads/{{ article.photos.[0] }}" style="height:250px" class="card-img-top rounded" alt="{{ article.name }} photo">
    </div>
    <div class="card-body py-2 px-3">
      <div class="my-1 d-flex justify-content-between align-items-center">
        <h4 class="card-title font-weight-bold m-0">{{ article.priceAppraiser }} €</h4>
        <div>
          {{!-- {{#isOwner session article }} --}}
            <a href="/articles/{{ article.id }}/edit" class="card-link"><i class="fas fa-pencil-alt fa-2x text.warning"></i></a>
            <a href="/articles/{{ article.id }}/delete" class="card-link"><i class="far fa-trash-alt fa-2x"></i></a>
         {{!--  {{else }} --}}
            <i class="far fa-heart fa-2x"></i>
          {{!-- {{/isOwner }} --}}
        </div>
      </div>
      <h5 class="card-title mb-3">{{ article.name }}</h5>
      <p class="card-text">{{ article.description }}</p>
    </div> 
  </a>
</div>
